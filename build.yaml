name: Build ZMK firmware

on: [push, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build-matrix: |
        { "include": [
          { "board": "nice_nano_v2", "shield": "handwired_tofu" }
        ] }